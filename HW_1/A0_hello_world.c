#include <stdio.h>

int main() 
{
    int num_one, num_two;
    scanf("%d", &num_one);  // Ввод числа
    scanf("%d", &numb_two);  // Ввод числа
    
    
    printf("%d\n", num_one + num_two);  // Вывод произведения
    
    return 0;
}
